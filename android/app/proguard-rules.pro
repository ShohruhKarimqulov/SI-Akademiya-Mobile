# Flutter plugins register through generated engine bindings. Keep annotations
# and signatures used by platform channels while allowing R8 to shrink the rest.
-keepattributes RuntimeVisibleAnnotations,RuntimeInvisibleAnnotations,Signature
